import styled from "styled-components";

export const Container = styled.div`
  position: fixed;
  height: 200%;
  width: 200%;
  top: -50%;
  left: -50%;
  overflow: hidden;
  z-index: 100;
  background: rgba(0, 0, 0, 0.3);
`;
